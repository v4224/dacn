apiVersion: v1
kind: Service
metadata:
  name: profile-service
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: profile
  ports:
    - port: 8081
      targetPort: 8081
