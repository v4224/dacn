apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  # nfs:
  #   path: /data/mysql
  #   server: 192.168.189.102
  persistentVolumeReclaimPolicy: Retain
  storageClassName: nfs-storage